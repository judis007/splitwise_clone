<%= form_with url: "/groups/#{@group_id}/expenses", method: "post" do |f| %>

    <%= f.label "Category: " %>
    <%= f.select :category, @categories %><br><br>

    <%= f.label "Currency: " %>
    <%= f.select :currency, currency_codes %><br><br>

    <%= f.label "Name the expense: " %>
    <%= f.text_field :name, placeholder: "Give a random name" %><br><br>

    <%= f.label "Total amount: " %>
    <%= f.text_field :expense %><br><br>

    <%= f.label "Paid by: " %>
    <%= f.collection_select :friend, @friends, :id, :name %><br>

    <%= f.submit %>
<% end %>